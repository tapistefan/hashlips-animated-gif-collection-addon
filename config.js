module.exports = {
    trait: "Background",
    background: true, // If set to "false" a replacement trait will be put in front of everything.
    resolutionWidth: 500, // must match resolution that you used when generating art with haslips art engine
    resolutionHeight: 500,
    debug: false,

}